---
title: SpaceAfterLogicalNot
---

# SpaceAfterLogicalNot (bool)

如果为 `true`，则在逻辑否操作符(`!`)后面插入一个空格。

=== "true"

    ```cpp
    ! someExpression();
    ```

=== "false"

    ```cpp
    !someExpression();
    ```
